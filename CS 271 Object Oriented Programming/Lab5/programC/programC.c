#include "functions.h"
#include <stdio.h>

int main(void) {
    printf("Answer to 2^4 is:   %lf\n", integerPower(2, 4));
    
    printf("Answer to 10^-1 is: %lf\n", integerPower(10, -1));
    
    printf("Answer to 17^0 is:  %lf\n", integerPower(17, 0));
    
}
